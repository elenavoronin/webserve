#include <iostream>       // For standard input/output operations
#include <sys/types.h>    // Defines data types used in system calls (e.g., `sockaddr_in`, `socklen_t`)
#include <sys/socket.h>   // Contains definitions for socket functions (e.g., `socket()`, `bind()`, `listen()`, `accept()`)
#include <netinet/in.h>   // Contains constants and structures needed for internet domain addresses (e.g., `sockaddr_in`)
#include <arpa/inet.h>    // Provides functions for manipulating numeric IP addresses (e.g., `inet_addr()`, `inet_ntoa()`)
#include <unistd.h>       // Provides access to POSIX operating system API (e.g., `close()` for closing sockets)

int main() {
	//server fd?
	return 0;
}